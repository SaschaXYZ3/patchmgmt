---
- name: Install Checkmk Raw Edition
  ansible.builtin.apt:
    deb: "{{ checkmk_download_url }}"
  when: ansible_os_family == "Debian"

- name: Ensure Checkmk service is enabled
  ansible.builtin.systemd:
    name: apache2
    enabled: true
    state: started